import styled from 'styled-components';
import tw from 'twin.macro';

export const Link = tw.a`text-6xl block`;

export const HeroContainer = styled.section`
  height: 100vh;
  display: flex;
  align-items: center;
`;

export const Title = tw.h2`text-6xl`;
export const TextWrapper = tw.div`py-20 text-center justify-center`;
